{
  "name": "privacy-first-intent-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json --declaration --sourceMap",
    "test": "npm run build && node --test tests/intent-sdk.test.mjs",
    "test:e2e": "npm run build && cypress run",
    "test:e2e:headed": "cypress open"
  },
  "devDependencies": {
    "cypress": "^13.15.2",
    "typescript": "^5.6.3"
  }
}
