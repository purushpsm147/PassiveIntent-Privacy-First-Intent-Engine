name: ci / react

on:
  push:
    branches: ['**']
    paths:
      - 'packages/react/**'
      - 'tsconfig.base.json'
      - 'package.json'
      - 'package-lock.json'
  pull_request:
    paths:
      - 'packages/react/**'
      - 'tsconfig.base.json'
      - 'package.json'
      - 'package-lock.json'

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20, 22]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck -w @edgesignal/react

      - name: Unit tests
        run: npm run test -w @edgesignal/react

      - name: Build
        run: npm run build -w @edgesignal/react
